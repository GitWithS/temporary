//local
const secret = 'Super Secret'

// share
const john = 'john'
const peter = 'peter'

// console.log(module)
// exports ids an object, whatever dump into the export can be accessed anywhere in the  application ;

module.exports = {john, peter} // exporting as objects
